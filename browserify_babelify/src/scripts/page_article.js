// import map from "lodash/map";

const articleTemplate = article => {
  return `<div>
    ${article}
  </div>`;
};

const alertTest = () => {
  console.log("alerting.....");
};

// const mapper = map([1, 2, 4], articleTemplate);

// console.log(mapper);
alertTest();
